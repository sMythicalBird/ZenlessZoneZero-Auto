# 此文件暂时用来参考连携按键，程序中暂未调用
# 鼠标左键按住0.1s，间隔0.5s
-   key: "left"          # 按键 left为鼠标左键，right为鼠标右键，middle为鼠标中键
    type: "press"        # 按住 press为按住， down为按下， up为松开
    duration: 0.1       # 按住时间  duration为按住时间，press时有效
    delay: 0.5          # 本次操作执行完毕后延迟时间 默认为0.1

# 鼠标左键按住0.1s，间隔0.5s
-   key: "left"          # 按键 left为鼠标左键，right为鼠标右键，middle为鼠标中键
    type: "press"        # 按住 press为按住， down为按下， up为松开
    duration: 0.1       # 按住时间  duration为按住时间，press时有效
    delay: 0.5          # 本次操作执行完毕后延迟时间 默认为0.1
